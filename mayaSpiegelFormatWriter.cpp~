
#include "mayaSpiegelFormatWriter.h"

using namespace std;


static bool mayaSpiegelFormatWriter::writeMSF( string inSimFileName, string outDir)
{

  
  int numOfParticles;
  double time;

  bool status = false;
  filebuf inSimFile;
  inSimFile.open (inSimFileName, ios::in);

  ifstream inStream(&inSimFile);
  
  
x
  
  
  return status;
}
